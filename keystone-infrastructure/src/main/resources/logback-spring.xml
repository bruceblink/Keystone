<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 日志文件存放路径 -->
    <springProperty name="log.path" source="logging.file.path" defaultValue="logs"/>

    <!-- 控制台日志 pattern，带默认值，防止 prod 环境缺失 -->
    <springProperty name="console.pattern" source="logging.pattern.console"/>
    <property name="console.pattern.default"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} requestId:%X{${keystone.traceRequestIdKey}} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- 文件日志统一 pattern -->
    <property name="log.pattern" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{20} - [%method,%line] - %msg%n"/>

    <!-- - 控制台输出 - -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${console.pattern:-${console.pattern.default}}</pattern>
            <charset>UTF-8</charset>
            <immediateFlush>true</immediateFlush> <!-- 确保立即刷到 STDOUT -->
        </encoder>
    </appender>

    <!-- - 文件日志输出 - -->
    <appender name="info_log" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/sys-info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/sys-info.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>INFO</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <appender name="error_log" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/sys-error.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/sys-error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <appender name="debug_log" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${log.path}/sys-debug.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${log.path}/sys-debug.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>${log.pattern}</pattern>
        </encoder>
    </appender>

    <!-- - 异步文件日志 - -->
    <appender name="async_info_log" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="info_log"/>
        <includeCallerData>true</includeCallerData>
    </appender>

    <appender name="async_error_log" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="error_log"/>
        <includeCallerData>true</includeCallerData>
    </appender>

    <appender name="async_debug_log" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>1024</queueSize>
        <discardingThreshold>256</discardingThreshold>
        <appender-ref ref="debug_log"/>
        <includeCallerData>true</includeCallerData>
    </appender>

    <!--- Root Logger -->
    <root level="info">
        <!-- 控制台直接输出，不使用 AsyncAppender 避免丢失 -->
        <appender-ref ref="console"/>
        <!-- 异步文件日志保持不变 -->
        <appender-ref ref="async_info_log"/>
        <appender-ref ref="async_error_log"/>
        <appender-ref ref="async_debug_log"/>
    </root>

</configuration>
