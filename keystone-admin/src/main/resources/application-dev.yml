# 开发环境配置
# 默认连接 localhost（Docker 暴露的端口）；在容器内运行时通过环境变量覆盖为容器名。
#
# 本地开发工作流：
#   1. cd docker && docker-compose up -d mysql redis   # 只启动基础设施
#   2. 直接在 IDE 中启动 KeystoneAdminApplication（SPRING_PROFILES_ACTIVE=dev）
#
# 全量 Docker 工作流：
#   docker-compose up -d    # docker-compose.yml 中的环境变量自动覆盖连接地址

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    dynamic:
      datasource:
        master:
          url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/keystone?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
          username: ${SPRING_DATASOURCE_USERNAME:root}
          password: ${SPRING_DATASOURCE_PASSWORD:12345}

  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      database: ${SPRING_DATA_REDIS_DATABASE:0}
      password: ${SPRING_DATA_REDIS_PASSWORD:12345}

logging:
  level:
    app.keystone: debug
  file:
    path: ${LOG_PATH:D:/logs/keystone-dev}

springdoc:
  swagger-ui:
    enabled: true
    url: ${keystone.api-prefix}/v3/api-docs
    config-url: ${keystone.api-prefix}/v3/api-docs/swagger-config

keystone:
  file-base-dir: ${KEYSTONE_FILE_BASE_DIR:D:\keystone}
  api-prefix: /dev-api
  demo-enabled: false
  frontend:
    url: ${KEYSTONE_FRONTEND_URL:http://localhost:80}
