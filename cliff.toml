# git-cliff configuration
# https://git-cliff.org/docs/configuration

[changelog]
# changelog header
header = ""
# template for the changelog body
# https://keats.github.io/tera/docs/#introduction
body = """
{% if version %}\
    ## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
    ## [unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
    ### &nbsp;&nbsp;&nbsp;{{ group }}
    {% for commit in commits %}
        - {% if commit.scope %}**{{ commit.scope }}**: {% endif %}\
          {{ commit.message | upper_first }} \
          &nbsp;-&nbsp; by @{{ commit.author.name }} \
          [<samp>({{ commit.id | truncate(length=7, end="") }})</samp>](https://github.com/bruceblink/Keystone/commit/{{ commit.id }})
    {%- endfor %}
{% endfor %}

"""
# remove the leading and trailing whitespace from the template
trim = true
# changelog footer
footer = """
{% if previous.tag and version %}\
##### &nbsp;&nbsp;&nbsp;&nbsp;[View changes on GitHub](https://github.com/bruceblink/Keystone/compare/{{ previous.tag }}...{{ version }})
{% endif %}\
"""
# postprocessors
postprocessors = []

[git]
# parse the commits based on https://www.conventionalcommits.org
conventional_commits = true
# filter out the commits that are not conventional
filter_unconventional = true
# process each line of a commit as an individual commit
split_commits = false
# regex for parsing and grouping commits
commit_parsers = [
    { message = "^feat", group = "🚀 Features" },
    { message = "^fix", group = "🐞 Bug Fixes" },
    { message = "^refactor", group = "♻️ Refactors" },
    { message = "^perf", group = "⚡ Performance" },
    { message = "^build", group = "⚡ Performance" },
    { message = "^test", group = "🧪 Testing" },
    { message = "^docs", group = "📝 Documentation" },
    { message = "^style", group = "🎨 Styling" },
    { message = "^chore", skip = true },
    { message = "^ci", skip = true },
    { message = "^revert", group = "⏪ Reverts" },
]
# protect breaking changes from being skipped due to matching a skipped commit_parser
protect_breaking_commits = false
# filter out the commits that are not matched by commit parsers
filter_commits = false
# regex for matching git tags
tag_pattern = "v[0-9].*"
# regex for skipping tags
skip_tags = "v0\\.1\\.0-beta\\.1"
# regex for ignoring tags
ignore_tags = ""
# sort the tags topologically
topo_order = false
# sort the commits inside sections by oldest/newest order
sort_commits = "newest"
